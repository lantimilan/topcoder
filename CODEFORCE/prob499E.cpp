#include <iostream>
using namespace std;

int main()
{
    int n, m; cin >> n >> m;
    int a[105];
    for (int i = 0; i < n; ++i) cin >> a[i];
    int first[105], second[105];
    for (int i = 0; i < m; ++i) {
        cin >> first[i] >> second[i];
    }
}
